## Required envs

```dotenv
DATABASE_URL='Url for postgres database'
SESSION_SECRET='Session token for solidstart'
EMAIL_USER='Email for nodemailer to conenct'
EMAIL_PASS='Password for nodemailer to connect'
GEOCODE_API_KEY='Api key for geocode.maps.co'
```